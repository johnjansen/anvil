# Implementation Plan: Remove Task Dependency Pipeline

**Branch**: `367-remove-dependency-pipeline` | **Date**: 2026-03-07 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/367-remove-dependency-pipeline/spec.md`

## Summary

Remove the broken `depends_on` task dependency pipeline feature entirely. The feature never worked correctly — dependencies only evaluated on their own cron tick, not event-driven. This is a pure removal: delete dependency types, parsing, resolution, validation, cycle detection, pipeline CLI command, and all related tests and documentation.

## Technical Context

**Language/Version**: Go 1.24.6
**Primary Dependencies**: `gopkg.in/yaml.v3` (frontmatter parsing — remains, just fields removed from structs)
**Storage**: JSON files in `.anvil/runs/<task-id>/` — no changes to run records
**Testing**: `go test ./...`
**Target Platform**: macOS/Linux CLI
**Project Type**: CLI tool with background daemon
**Performance Goals**: N/A (removal only)
**Constraints**: Must not break existing task files that contain stale `depends_on` fields
**Scale/Scope**: ~13 files affected, ~800 lines removed

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

No constitution configured (template placeholders only). No gates to enforce.

## Project Structure

### Documentation (this feature)

```text
specs/367-remove-dependency-pipeline/
├── spec.md
├── plan.md              # This file
├── research.md          # Code inventory and decisions
├── data-model.md        # Struct changes
└── tasks.md             # Task breakdown (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
internal/
├── project/
│   ├── dependencies.go      # DELETE entirely
│   ├── dependencies_test.go # DELETE entirely
│   └── project.go           # MODIFY: remove DependsOn fields from Todo, frontmatter, generation
├── daemon/
│   └── daemon.go            # MODIFY: remove checkDependenciesMet, dep collection, validation
cmd/anvil/
├── task_pipeline.go         # DELETE entirely
├── task_create.go           # MODIFY: remove --depends-on flag
├── task_results.go          # MODIFY: remove dependency results display
├── task_list.go             # MODIFY: remove dependency display
├── task_router.go           # MODIFY: remove pipeline route
├── main.go                  # MODIFY: remove pipeline from help
└── dryrun.go                # MODIFY: remove dependency section
tools/skills/anvil/
└── SKILL.md                 # MODIFY: remove depends_on and pipeline docs
CLAUDE.md                    # MODIFY: remove dependency-related technology entries
```

**Structure Decision**: No new files or directories. This is purely a removal operation across existing files.

## Complexity Tracking

No violations — this is a straightforward removal with no new complexity introduced.
