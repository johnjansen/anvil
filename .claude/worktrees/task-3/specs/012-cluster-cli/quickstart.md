# Quickstart: Cluster CLI Commands

## Prerequisites

- anvil daemon running with cluster mode enabled
- At least one peer configured in cluster settings

## Usage

### View cluster status
```bash
anvil cluster status
anvil cluster status --json
```

### Check cluster health
```bash
anvil cluster health
anvil cluster health --json
```

### Leave the cluster
```bash
anvil cluster leave
```
